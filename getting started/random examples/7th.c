#include<stdio.h>
int main()
{
{
    float a,b,square();
    printf("enter any no:");
    scanf("%f",&a);
    b=square(a);
    printf("final value of %f is %f",&a,&b);
}
{
    square(x);
    square(x)=y;
    y=a*a;
    return f;
}
}